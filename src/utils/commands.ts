import { TypeCommands } from "../types"

const commands: TypeCommands[] = [
    'users',
    'cat',
    'dog',
    'valval',
    'roles',
    'help',
    'pixel-art',
    'image',
    'pin'
]

export default commands